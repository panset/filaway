---
id: 6B1A5900-CD72-4AFA-8DB1-8F324DFFC478
created: 2026-04-25T10:55:21Z
modified: 2026-05-08T10:55:21Z
---
# Untitled 163

The container starts, the pod is ready, and the gateway is still wrong. I want a note that just holds the command, not an essay about the gateway.

The gateway came up again and nobody could remember what we decided last time. The tricky part with the gateway is that it only misbehaves when the machine is loaded.

Logs from the deploy are useless here — nothing about the gateway is written out at all. The thing I actually needed was two directories up, filed under the gateway.

## Next

Worth benchmarking before touching the gateway; the last guess was off by an order of magnitude.

Certificates, tokens and clock skew: three explanations for the same symptom in the gateway. Logs from the deploy are useless here — nothing about the gateway is written out at all.

Certificates, tokens and clock skew: three explanations for the same symptom in the gateway. Worth benchmarking before touching the gateway; the last guess was off by an order of magnitude.

