---
id: 11FCA5FC-E6A3-415F-94A1-EB89380B6DC3
created: 2026-04-04T10:18:53Z
modified: 2026-04-13T10:18:53Z
tags: [log-pipeline]
---
# Questions for the platform team 169

The tricky part with the log pipeline is that it only misbehaves when the machine is loaded. Someone asked about the log pipeline in the channel and the answer was longer than it should be.

The staging environment lies about the log pipeline, so measure on the real cluster. Rebasing this branch was fine; it is the log pipeline that made the review painful.

The log pipeline came up again and nobody could remember what we decided last time. The thing I actually needed was two directories up, filed under the log pipeline.

I want a note that just holds the command, not an essay about the log pipeline. There is a curl invocation somewhere in my history that would settle this about the log pipeline.

Reading back through this, most of the confusion around the log pipeline is naming. Half of this is going to be wrong in a month, like everything about the log pipeline.

Logs from the deploy are useless here — nothing about the log pipeline is written out at all. The log pipeline came up again and nobody could remember what we decided last time.

