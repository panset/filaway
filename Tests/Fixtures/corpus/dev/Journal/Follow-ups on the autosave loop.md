---
id: 9CE6C712-C5BA-4534-84A6-0DBFAA3A9BCC
created: 2026-02-20T11:25:41Z
modified: 2026-03-03T11:25:41Z
---
# Follow-ups on the autosave loop

I want a note that just holds the command, not an essay about the autosave loop. The tricky part with the autosave loop is that it only misbehaves when the machine is loaded.

There is a curl invocation somewhere in my history that would settle this about the autosave loop. Certificates, tokens and clock skew: three explanations for the same symptom in the autosave loop.

The docs for the autosave loop describe the version before last, which cost me an hour. The container starts, the pod is ready, and the autosave loop is still wrong.

Worth benchmarking before touching the autosave loop; the last guess was off by an order of magnitude. I keep meaning to write down how the autosave loop is actually wired, and keep not doing it.

The autosave loop came up again and nobody could remember what we decided last time. Worth benchmarking before touching the autosave loop; the last guess was off by an order of magnitude.

