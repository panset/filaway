---
id: EAE31CB0-9CA0-4BB7-9D73-1BC6A7AA8A85
created: 2026-07-10T11:50:14Z
modified: 2026-07-29T11:50:14Z
tags: [autosave-loop]
---
# the autosave loop — open questions

The autosave loop came up again and nobody could remember what we decided last time. I keep meaning to write down how the autosave loop is actually wired, and keep not doing it.

We agreed to revisit the autosave loop once the migration is finished, so probably never. The thing I actually needed was two directories up, filed under the autosave loop.

The staging environment lies about the autosave loop, so measure on the real cluster. I want a note that just holds the command, not an essay about the autosave loop.

- [ ] book time to look at the autosave loop properly
- [ ] ask the platform team about the autosave loop

We agreed to revisit the autosave loop once the migration is finished, so probably never. I want a note that just holds the command, not an essay about the autosave loop.

Someone asked about the autosave loop in the channel and the answer was longer than it should be. The tricky part with the autosave loop is that it only misbehaves when the machine is loaded.

