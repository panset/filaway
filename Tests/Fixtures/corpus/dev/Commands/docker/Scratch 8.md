---
id: 86BC35A2-15F8-48D2-8083-4D0C59282C72
created: 2026-06-12T11:20:16Z
modified: 2026-06-14T11:20:16Z
---
# Scratch 8

Logs from the deploy are useless here — nothing about the release branch is written out at all. The thing I actually needed was two directories up, filed under the release branch.

We agreed to revisit the release branch once the migration is finished, so probably never. Every time the token expires I go looking for the same thing about the release branch.

The staging environment lies about the release branch, so measure on the real cluster. The thing I actually needed was two directories up, filed under the release branch.

Someone asked about the release branch in the channel and the answer was longer than it should be. Every time the token expires I go looking for the same thing about the release branch.

Logs from the deploy are useless here — nothing about the release branch is written out at all. There is a curl invocation somewhere in my history that would settle this about the release branch.

## What I tried

The staging environment lies about the release branch, so measure on the real cluster.

